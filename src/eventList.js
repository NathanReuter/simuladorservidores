(function () {
    "use strict";

    var EventList = function () {
        this.list = [];
    };

    EventList.prototype.addEvent = function () {
    //    Add entity and sort by TC
    };

    EventList.prototype.nextEvent = function () {
    // Just get the head of the list
    };

    module.exports = EventList;
})();